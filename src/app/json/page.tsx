"use client"

import React from 'react';
import {Button} from "@/components/ui/button";
export type JsonProps = {}

export default function Page({}: JsonProps) {

    return (<div>
        <Button>确认</Button>
    </div>);
}
